{% extends 'base.html.twig' %}

{% block title %}Job{% endblock %}

{% block body %}
    <h1>Job</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job.id }}</td>
            </tr>
            <tr>
                <th>Organisation</th>
                <td>{{ job.organisation }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ job.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ job.description }}</td>
            </tr>
            <tr>
                <th>Startdate</th>
                <td>{{ job.startdate ? job.startdate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_front_job_index') }}">back to list</a>

    <a href="{{ path('app_front_job_edit', {'id': job.id}) }}">edit</a>

    {{ include('front_job/_delete_form.html.twig') }}
{% endblock %}
