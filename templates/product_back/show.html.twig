{% extends 'baseback.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}


    <h1>Product</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Product name</th>
                <td>{{ product.productname }}</td>
            </tr>
            <tr>
                <th>Product quantity</th>
                <td>{{ product.productquantity }}</td>
            </tr>
            <tr>
                <th>Product size</th>
                <td>{{ product.productsize }}</td>
            </tr>
            <tr>
                <th>Product price</th>
                <td>{{ product.productprice }}</td>
            </tr>
            <tr>
                <th>Product description</th>
                <td>{{ product.productdescription }}</td>
            </tr>
            <tr>
                <th>Product Image</th>
                <td>{{ product.productimg }}</td>
            </tr>


            <tr>
                <th>Product disponibility</th>
                <td>{{ product.productdisponibility }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_back_index') }}">back to list</a>

    <a href="{{ path('app_product_back_edit', {'id': product.id}) }}">edit</a>

    {{ include('product_back/_delete_form.html.twig') }}
{% endblock %}
