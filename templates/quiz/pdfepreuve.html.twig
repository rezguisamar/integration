<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Report</title>
    <img src="{{ asset('public/assets/img/feuilles.png') }}" alt="greenta">

    <style>
        /* Define your styles for the PDF here */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            color: #333;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        header, footer {
            background-color: #008080; /* Greenta site color */
            color: #fff;
            padding: 10px;
            text-align: center;
            border-radius: 10px 10px 0 0;
        }
        footer {
            margin-top: 20px;
            border-radius: 0 0 10px 10px;
            font-size: 12px;
        }
        h1, h2 {
            text-align: center;
            color: #008080; /* Greenta site color */
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 10px;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>

    <div class="container">
        <header>
            <h1>Greenta Quiz Report</h1>
        </header>
        <center> <h2>QUIZ CERTIFICATION </h2>
        <h2>Congratulations, you passed Quiz {{epreuve.quizid.titre}}</h2></center>
        
        <h3>Questions report card</h3>
        <table>
            <thead>
                <tr>
                    <th>Question</th>
                    <th>Response</th>
                    <th>Note</th>
                </tr>
            </thead>
            <tbody>
                {% for quest in epreuve.quizid.questions %}
                    <tr>
                        <td>{{ quest.question }}</td>
                        <td>{{ quest.reponse }}</td>
                        <td>{{ quest.note }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <h3>Result obtained: {{epreuve.note}}</h3>
        <footer>
            <p>This report is generated by Greenta. &copy; 2024 Greenta. All rights reserved.</p>
        </footer>
    </div>

</body>
</html>
