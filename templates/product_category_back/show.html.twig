{% extends 'baseback.html.twig' %}

{% block title %}Product Category{% endblock %}

{% block body %}
    <h1>Product Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_category.id }}</td>
            </tr>
            <tr>
                <th>Category name</th>
                <td>{{ product_category.categoryname }}</td>
            </tr>
            <tr>
                <th>Category image</th>
                <td>{{ product_category.categoryimage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_category_back_index') }}">back to list</a>

    <a href="{{ path('app_product_category_back_edit', {'id': product_category.id}) }}">edit</a>

    {{ include('product_category_back/_delete_form.html.twig') }}
{% endblock %}
